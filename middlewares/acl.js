const checkAccessToUpdate = (req, res, next) => { // eslint-disable-line
  // TODO implement with boss service
  next();
};
const checkAccessToView = (req, res, next) => { // eslint-disable-line
  // TODO implement with boss service
  next();
};
const checkAccessToChangeBoss = (req, res, next) => { // eslint-disable-line
  // TODO implement with boss service
  next();
};

module.exports = {
  checkAccessToUpdate,
  checkAccessToView,
  checkAccessToChangeBoss,
};
